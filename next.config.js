/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["samuraistarter.com", "samuraistarter.mypinata.cloud"],
  },
};

module.exports = nextConfig;
